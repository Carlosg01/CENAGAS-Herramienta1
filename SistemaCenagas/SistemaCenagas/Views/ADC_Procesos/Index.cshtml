
@{
    ViewData["Title"] = "Index";
}

<div class="main-container">

    <div class="navbar bg-white breadcrumb-bar">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a asp-controller="Home" asp-action="Dashboard">Inicio</a>
                </li>
                @if (Global.session_usuario.user.Id_Usuario == Global.adc.adc.Id_ProponenteCambio)
                {
                    <li class="breadcrumb-item">
                        <a asp-controller="ADCUsuario" asp-action="Index">ADC</a>
                    </li>
                }
                else
                {
                    <li class="breadcrumb-item">
                        <a asp-controller="ADCAdmin" asp-action="Index">ADC</a>
                    </li>
                }
                <li class="breadcrumb-item active" aria-current="page">Tareas</li>

            </ol>
        </nav>

        <div class="dropdown">
            <button class="btn btn-round" role="button" title="@(Global.session_usuario.nombre_completo)" data-toggle="dropdown" aria-expanded="false">
                <img alt="Image" src="~/assets/img/user.jpeg" class="avatar" />
            </button>
            <div class="dropdown-menu dropdown-menu-right">
                <a class="dropdown-header text-dark text-uppercase">@(Global.session_usuario.Rol)</a>
                <a class="dropdown-item" asp-controller="Home" asp-action="AccountSettings">Configuración de la cuenta</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item text-danger" asp-controller="Home" asp-action="LogOut">Cerrar sesión</a>

            </div>
        </div>

    </div>
    <div class="container">

        <div id="PlaceHolderHere"></div>

        <div class="page-header mb-4">
            <div class="media">
                <div class="media-body ml-3">
                    <h3 class="mb-0">Tareas para la administración de cambio</h3>
                </div>
            </div>
        </div>

        <div class="card">
            <div class="card-body">
                <div class="tab-content">
                    <div class="tab-pane fade show active" role="tabpanel" id="profile">
                        <!--end of avatar-->
                        <dl class="row">
                            <dt class="col-sm-3">
                                Residencia:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.anexo1.residencia)
                            </dd>
                            <dt class="col-sm-3">
                                Proyecto:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.anexo1.proyecto)
                            </dd>
                            <dt class="col-sm-3">
                                Instalación/Área:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.anexo1.instalacion)
                            </dd>
                            <dt class="col-sm-3">
                                Tipo de cambio:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.anexo1.anexo1.Tipo_Cambio)
                            </dd>
                            <dt class="col-sm-3">
                                No. de Folio ADC:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.adc.Folio)
                            </dd>
                            <dt class="col-sm-3">
                                Proponente del cambio:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.proponente)
                            </dd>
                            <dt class="col-sm-3">
                                Responsable de la ADC:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.responsable)
                            </dd>
                            <dt class="col-sm-3">
                                Suplente:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.suplente)
                            </dd>
                            <dt class="col-sm-3">
                                Líder:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.lider)
                            </dd>
                            <dt class="col-sm-3">
                                Fecha de actualización:
                            </dt>
                            <dd class="col-sm-9">
                                @(Global.adc.adc.Fecha_Actualizacion)
                            </dd>
                        </dl>
                    </div>
                </div>
            </div>
        </div>

        <table id="example" class="table table-striped table-bordered" style="width:100%">
            <thead class="thead-dark">
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Actividad
                    </th>
                    <th>
                        Avance
                    </th>
                    <th>
                        Faltante, Comentarios
                    </th>
                    <th>
                        Plan de acción
                    </th>
                    <th>Terminado</th>
                    <th>Confirmado</th>
                    <th></th>
                    <th></th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>



            <tbody>
                <!--SOLICITUD DE CAMBIO-->
                @{ int index = 0;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--ANALISIS DE LA PROPUESTA DE CAMBIO-->
                @{ index = 1;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Evaluación del Análisis de Riesgo de Proceso correspondiente a la Administración del Cambio-->
                @{ index = 2;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                 Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Evaluación Preliminar de Riesgos-->
                @{ index = 3;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                  Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Resultado del análisis técnico-económico-->
                @{ index = 4;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                   Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Revisión de seguridad Pre-Arranque-->
                @{ index = 5;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                    Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Autorización para el inicio de operación-->
                @{ index = 6;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                     Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Cierre de la Administración del Cambio-->
                @{ index = 7;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                      Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>

                <!--Control de cambios de centro de trabajo-->
                @{ index = 8;}
                <tr>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)
                    </td>
                    <td>
                        <a asp-action="Normativas" asp-route-id="@(Global.vista_tareas.ElementAt(index).proceso.Id_Actividad)">
                            @(Global.vista_tareas.ElementAt(index).actividad)
                        </a>
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Avance) %
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Faltante_Comentarios)
                    </td>
                    <td>
                        @(Global.vista_tareas.ElementAt(index).proceso.Plan_Accion)
                    </td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true")?"Si":"No")</td>
                    <td>@(Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true") ? "Si" : "No")</td>
                    <td>
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: green; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if ((Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("true") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false")) ||
                       Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("true"))
                        {
                            <span style="height: 25px; width: 25px; background-color: darkorange; border-radius: 50%; display: inline-block;"></span>
                        }
                        @if (Global.vista_tareas.ElementAt(index).proceso.Terminado.Equals("false") && Global.vista_tareas.ElementAt(index).proceso.Confirmado.Equals("false"))
                        {
                            <span style="height: 25px; width: 25px; background-color: red; border-radius: 50%; display: inline-block;"></span>
                        }

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Editar actividad"
                                data-toggle="modal" data-target="#editarProceso-modal" data-url="@Url.Action($"Edit/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}")">
                            <span class="material-icons">edit</span>
                        </button>

                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Gestionar archivos"
                                data-toggle="modal" data-target="#archivoProceso-modal" data-url="@Url.Action($"Archivos/{Global.vista_tareas.ElementAt(index).proceso.Id_Proceso}", "Anexo1")">
                            <span class="material-icons">attach_file</span>
                        </button>
                    </td>
                    <td style="width:15%;">
                        <button class="btn btn-round" title="Anexo 1"
                                data-toggle="modal" data-target="#editarAnexo1-modal" data-url="@Url.Action($"Edit/{Global.adc.adc.Id_ADC}", "Anexo1")">
                            <span class="material-icons">exit_to_app</span>
                        </button>
                    </td>


                    <!--td style="width:15%;">
                <a class="btn btn-round form-control" title="Descargar reporte" asp-action="Anexo1_Descargar" asp-route-idADC="@(Global.adc.adc.Id_ADC)">
                    <span class="material-icons">get_app</span>
                </a>
            </td-->
                </tr>


            </tbody>
            <tfoot>
                <tr>
                    <td></td>
                    <td>
                        <label style="color:red;">Avance total:</label>
                    </td>
                    <td>
                        <label style="color:red;"><b>@(ViewBag.avance_total / Global.vista_tareas.Count()) %</b></label>
                    </td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
            </tfoot>
        </table>

    </div>
</div>
