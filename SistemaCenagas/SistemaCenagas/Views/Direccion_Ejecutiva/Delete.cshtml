@model SistemaCenagas.Models.Direccion_Ejecutiva

@{
    ViewData["Title"] = "Delete";
}

<div class="modal fade" tabindex="-1" role="dialog" id="eliminarUsuario-modal">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Eliminar dirección ejecutiva</h5>
                <a asp-action="Index" class="close btn btn-round">
                    <i class="material-icons">close</i>
                </a>
            </div>
            <!--end of modal head-->
            <div class="modal-body">

                <form asp-action="Delete">
                    <div class="card card-note">
                        <div class="card-body">
                            <p>¿Estás seguro de eliminar la dirección ejecutiva <b>@(Model.Nombre)</b>?</p>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <input type="hidden" asp-for="Id" />
                        <button role="button" class="btn btn-danger" type="submit">
                            Eliminar
                        </button>
                        <a class="btn btn-dark" asp-action="Index">
                            Cancelar
                        </a>
                    </div>
                    
                </form>

            </div>
        </div>
    </div>
</div>
